#pragma once

#include <Windows.h>

#include <psapi.h>

#include <iphlpapi.h>

#include <assert.h>

#include <tchar.h>

#include <stdio.h>

#pragma comment(lib, "iphlpapi.lib")

typedef enum LOG_LEVEL LOG_LEVEL;

typedef enum QUIT_CODE QUIT_CODE;
